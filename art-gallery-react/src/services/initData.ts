// File này từng dùng để seed dữ liệu mẫu vào localStorage trước khi
// dự án chuyển sang gọi backend thật. Hiện tại tất cả dữ liệu đều lấy
// từ API ASP.NET Core, nên không cần seed gì nữa.
// Hàm để trống để khỏi phá hỏng các import hiện có.

export const initializeData = (): void => {
  // No-op
};
